{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "start nodemon server.js && yarn --cwd client start",
    "start": "node server.js",
    "heroku-prebuild": "npm install --prefix client && npm run-script build --prefix client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-jwt": "^6.0.0",
    "jwks-rsa": "^1.11.0",
    "mongodb": "^3.6.2",
    "mongoose": "^5.10.9",
    "passport": "^0.4.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.5"
  }
}
